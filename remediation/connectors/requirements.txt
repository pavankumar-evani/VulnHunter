# Supply-chain guardrail (OWASP LLM Top 10 2026 #4) - pinned exact, see
# dashboard/requirements.txt's own comment for why and how to bump these.
requests==2.34.2
ldap3==2.9.1  # real Active Directory/LDAP asset-inventory pulls (active_directory_connector.py) - already a project dependency, see dashboard/auth/ad_directory.py
python-gvm>=24.0  # real OpenVAS/Greenbone (GMP protocol) scan-engine connector (openvas_connector.py) - left unpinned: never actually installed/verified in this environment (see openvas_connector.py's own "never exercised against a real GVM server" disclosure) - pin this to a specific verified version once it's really been run once, rather than fabricate a "verified" number now
